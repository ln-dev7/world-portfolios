import data from "./data";

// Order the data by name
const dataOrder = data.sort((a, b) => {
  if (a.name < b.name) {
    return -1;
  }
  if (a.name > b.name) {
    return 1;
  }
  return 0;
});

export default dataOrder;
